#include "main.h"

/**
 * print_most_numbers - Function
 *
 */
void print_most_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		if ((n != 2) && (n != 4))
			_putchar(n + '0');
	}
	_putchar('\n');
}
