#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: character thats checks for digit
 * Return: 0 or 1
 */
int _isdigit(int c)
{
	if c = 0
	{
		printf("%d\n", c);
		return (1);
	}
	else if c = a
	{
		printf("%d\n", c);
		return (0);
	}
}
