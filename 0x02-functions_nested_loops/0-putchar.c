#include <stdio.h>
#include <main.h>

/**
 * main - prints _putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char(_);
	char(p);
	char(u);
	char(t);
	char(c);
	char(h);
	char(a);
	char(r);
}
_putchar('\n');
return (0);
