#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result.
 * @num1: first variable
 * @num2: second variable
 * Return: result
 */
int add(int num1, int num2)
{
	int i;
	 i = num1 + num2;
	return (i);
}
